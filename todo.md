# 独立仓库交付

- [x] 取消个人额度读取对单独安装 Codex CLI 的强制依赖：优先定位并核验桌面版自带程序，保留 PATH CLI 兼容及明确的路径覆盖；覆盖未安装、可用、失效与取消边界，同步双语安装说明，以不依赖 PATH 中 codex 的真实周额度读取验收。不改悬浮样式、CDP 安全边界、Key、概率算法，不自动重启或发布。
  - 新增运行时定位：当前桌面实例、常见安装目录、显式绝对路径以及 PATH 回退；检查应用身份和包内可执行文件，元数据检查不传递会员凭证。无效配置或多个匹配应用时，普通查询和悬浮框均明确提示，不静默使用其他账号数据。
  - 本机实测在仅含系统工具的 PATH 下，自动使用桌面版内置程序读取真实通用周额度成功；未读取或迁移登录文件，未启动模型。npm test 162/162、npm run check、插件/Skill 校验和差异检查通过，独立审查通过。仅本地源码与文档提交，未推送、未替换已安装 GitHub 插件、未重启当前悬浮框。

- [x] 在用户本机按公开 README 从 GitHub 安装并验收：核对 Node/Codex 登录，安装远程 marketplace 与插件、避免旧 personal 版重复启用，保留原 Key；从实际安装目录验证查询与悬浮连接，不自动重启 Codex、不建立额外定时任务、不修改产品代码。安装、真实数据和界面结果分别核对后记录。
  - Node25/Codex CLI0.143及ChatGPT登录正常；公开 Git marketplace 安装成功，仅 reset-radar@reset-radar 启用，旧 personal 安装记录与缓存已移除，旧源码和原 Key 保留且 Key 权限仍600。安装包34文件与公开提交2bdd996完全一致，开发者唐卡/Tangka。
  - 直接运行已安装 personal 命令，会员 API与本机通用周额度查询成功；复用原有兼容CDP会话，无需重启。通过正常关闭按钮结束旧卡片，在外部终端运行已安装 overlay start，新会话实际显示个人/额外概率、Codex周用量和倒计时。重开时沿用上次自动刷新间隔，点击一次现有刷新按钮后取数成功、忙碌状态恢复，无限流绕过或Key重写。普通新任务的技能自动拾取留待用户新建任务使用，不声称已另建任务验收。
  - npm test147/147、npm run check及差异检查通过；仅记录本机安装验收，不将个人用量、配置或凭证写入仓库，不推送本机记录、不发布产品改动。

- [x] 将根 README.md 设为英文默认说明，新增独立 README.zh-CN.md 中文版并互相链接；保留署名、所有安装/配置/更新命令、会员边界和非官方悬浮风险，核验双语内容及测试后单独提交，仅改文档。
  - 中文版除顶部语言链接外与原中文逐字一致；英文版独立语义复核通过，8 个命令块（占位符翻译后）、章节数、署名与本地链接核验一致。npm test 147/147、npm run check 与差异检查通过，未改运行代码、元数据或本机安装。

- [x] 按用户最新署名“唐卡/Tangka”更新插件作者、开发者、README 和对应断言；只改署名，测试与元数据校验通过后单独提交，保持现有功能与本机安装不变。
  - 三处显示署名及两条断言完全一致；npm test 147/147、npm run check、插件元数据验证与 git diff --check 通过，未改运行代码、版本号或本机安装。

- [x] 按用户提供的 tangka/reset-rader 远程地址更新安装说明，保留 reset-radar 插件及 marketplace 标识；测试、源码和差异检查通过后独立提交，不修改插件功能或现有安装。
  - README 已替换为实际 GitHub 地址，明确仓库名与插件安装标识不同；npm test 147/147、npm run check、打包边界3/3与 git diff --check 通过，生产代码无修改。
- [x] 将独立仓库关联并正常推送到用户指定的 https://github.com/tangka/reset-rader.git：先确认远程为空且无冲突，推送后回读分支提交、公开文件与干净克隆检查，再记录核验结果；不强推、不改其他仓库或运行服务。
  - 远程原为空公开仓库，首次正常推送 main 后提交回读与本地 4f31da1 一致；GitHub 默认分支 main、公开且非空。禁用 Git 凭证助手的独立克隆成功，克隆副本 npm test 147/147、npm run check 和打包边界3/3通过；40文件分发审查无真实凭证或私有数据阻断。未改现有插件安装和悬浮进程。
- [x] 按用户追加要求，将公开插件作者和开发者统一为“唐卡”，同步 README 与元数据校验；仅改署名不改插件功能，测试后独立提交并推送当前远程，回读公开文件确认。
  - 署名提交 fed6231 已正常推送；GitHub main 提交与本地一致，公开 manifest 的 author.name / interface.developerName 均为“唐卡”，README 署名及实际安装命令回读通过。npm test 147/147、npm run check、插件元数据验证及差异检查通过；未改个人安装、悬浮运行或线上会员服务。

- [x] 从已提交插件提取独立仓库，保留现有功能；提供 repo marketplace、安装/配置/更新/安全说明，验证独立测试、源码检查及打包边界后初始提交。不包含原主仓库历史、生产凭证或用户数据；不改现有安装，不创建或推送远程。
  - 34 个插件文件与来源提交 2d7b578 逐字一致，含卡片内 Key 配置、周额度用量与手动刷新；稳定版本为 0.1.0，不包含原工作区临时缓存版本号。
  - 本仓库与独立临时目录的 npm test 均 147/147，npm run check、插件/Skill/marketplace 验证和 CLI --help 均通过；没有修改真实 Codex 安装或为测试读取用户 Key。源码检查无跨仓库依赖与真实凭证，测试夹具不充当线上数据。
  - 来源仓库插件 144/144、采集器 595/595 与 178 源码检查、小程序/云端 784/784 通过；仅打包和文档有变化，不据此声称另一台电脑或所有客户端版本已验收。远程发布仍待授权。
