{
  "name": "reset-radar-plugin-repository",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node plugins/reset-radar/check-source.mjs && cd tests && node --test"
  }
}
